name: Asan notification source backup only
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  backup-info:
    runs-on: ubuntu-latest
    steps:
      - name: No notification scheduling in GitHub
        run: echo 'App notifications are scheduled by Cloudflare Durable Object Alarms. GitHub is backup only.'
