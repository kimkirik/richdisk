name: Asan rental source backup only
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  backup-info:
    runs-on: ubuntu-latest
    steps:
      - name: Live collection is managed outside GitHub
        run: echo 'This repository is a source backup. Production collection and push run on the independent Cloudflare server.'
