{
  "name": "neon-paw-play",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=./",
    "check": "node --check app.js && node --check sw.js"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
